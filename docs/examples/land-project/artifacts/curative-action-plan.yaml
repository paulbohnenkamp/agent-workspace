kind: artifact-type
id: curative-action-plan
name: Curative Action Plan
version: 1.0.0
description: Prioritized plan for resolving title or rights findings.
schema:
  type: object
  properties:
    matter_id: {type: string}
    findings: {type: array, items: {type: string}}
    actions: {type: array, items: {type: string}}
    owners: {type: array, items: {type: string}}
    target_dates: {type: array, items: {type: string}}
    dependencies: {type: array, items: {type: string}}
    escalation_required: {type: boolean}
metadata: {tags: [title, curative, workflow]}
