# Workspace Shell

The workspace shell is composed from zones.

```text
Header
Queue
Assistant Surface
Artifact Surface
Knowledge Panel
Agent Panel
Action Panel
Activity Timeline
Modal Surface
```

Each zone is bound to object kinds through the workspace definition.

The shell MUST be able to render different workspace types without hard-coding domain-specific pages.
