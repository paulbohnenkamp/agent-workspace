{
  "name": "agent-workspace-platform",
  "version": "0.0.1",
  "description": "Agent Workspace Platform - metadata-driven collaborative AI workspaces",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@awp/types && npm run build --workspace=@awp/loader && npm run build --workspace=@awp/tools && npm run build --workspace=@awp/runtime && npm run build --workspace=@awp/schemas",
    "build:system": "tsc -p tsconfig.system.json",
    "test": "jest --config ./jest.config.cjs",
    "system": "npm run build:system && node build/system/system/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.5",
    "json-schema-to-typescript": "^13.0.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
